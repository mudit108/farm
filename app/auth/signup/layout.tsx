import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create your account | Mera Khet",
  description: "Create a Mera Khet account to reserve your wheat plots in Sujangarh, Rajasthan.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
